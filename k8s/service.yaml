apiVersion: v1
kind: Service
metadata:
  name: shopcart
spec:
  selector:
    app: shopcart
  type: ClusterIP
  internalTrafficPolicy: Local
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
